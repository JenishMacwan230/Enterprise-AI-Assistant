# repositories package
# Data-access logic lives here — each repository wraps one ORM model.
# Repositories only run queries; no business rules.
# Phase 2 (Authentication) will add: user_repository.py
