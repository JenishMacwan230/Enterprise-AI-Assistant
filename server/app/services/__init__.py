# services package
# Business logic lives here — never in routes.
# Services call repositories; repositories talk to the database.
# Phase 2 (Authentication) will add: auth_service.py
# Phase 3 (User Management) will add: user_service.py
